import React from 'react';

const Square = ({ item }) => {
  return (
    <div>
      {/* <button onClick={() => console.log(item.name)}>{item.value}</button> */}
    </div>
  );
};

export default Square;
